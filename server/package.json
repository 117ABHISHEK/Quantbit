{
  "name": "quantbit-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx index.ts",
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongodb": "^5.7.0",
    "pdfkit": "^0.17.2",
    "date-fns": "^3.6.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "esbuild": "^0.25.0",
    "cross-env": "^7.0.3"
  }
}
