🏭 Build a complete Smart Factory Maintenance Tracker web application using
Node.js (Express) for the backend, React + Bootstrap 5 for the frontend, and MongoDB (Mongoose) for the database.

⚙️ App Goal

Help factories track machine maintenance logs and schedules to avoid breakdowns.

🧩 Core Modules

1. Machine Management

Add / Edit / Delete machines.

Each machine has: name, type, lastMaintenance, nextMaintenance, status.

2. Maintenance Logs

Record machineId, performedBy, date, notes.

When a log is created:
• Update machine lastMaintenance = log.date
• nextMaintenance = lastMaintenance + 30 days
• Auto-set status: Overdue / Due Soon / OK.

3. Dashboard

Three cards: Healthy Machines, Due Soon, Overdue.

A table listing machines with color status badges (green/yellow/red).

Action buttons (Edit, Delete, View Logs).

Modal form to add new maintenance entry.

4. Reports

Endpoint /api/reports/pdf to generate a PDF listing all machines and their next maintenance dates (using pdfkit).

🗂️ Project Structure to Generate
smart-factory-tracker/
├── backend/
│   ├── server.js
│   ├── models/
│   │   ├── Machine.js
│   │   ├── MaintenanceLog.js
│   ├── routes/
│   │   ├── machineRoutes.js
│   │   ├── maintenanceRoutes.js
│   ├── utils/dateHelper.js
│   ├── seed.js
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── DashboardCards.jsx
│   │   │   ├── MachineTable.jsx
│   │   │   ├── AddMaintenanceModal.jsx
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx
│   │   │   ├── Machines.jsx
│   │   ├── api.js
│   │   ├── App.jsx
│   │   ├── index.jsx
│   └── public/
└── package.json

🧠 Backend Details

Use Express with CORS and dotenv.

Use Mongoose to connect to MongoDB (MONGO_URL).

Endpoints:

GET /api/machines → list machines

POST /api/machines → add machine

PUT /api/machines/:id → update machine

DELETE /api/machines/:id → delete

POST /api/maintenance → add log and update machine dates/status

GET /api/maintenance/overdue → machines past due

GET /api/reports/pdf → generate PDF report

Include a seed.js file that inserts five sample machines with different dates.

🖥️ Frontend Details

React with Bootstrap 5 and Axios.

Dashboard.jsx shows summary cards + table.

AddMaintenanceModal.jsx for form input with validation.

Use react-toastify for notifications (Success/Fail).

Connect to backend via http://localhost:5000/api.

🧩 Utilities

dateHelper.js function calculateNextMaintenance(lastDate) adds 30 days.

Automatic status logic:

const today = new Date();
if (next < today) status = "Overdue";
else if ((next - today) / (1000*60*60*24) <= 3) status = "Due Soon";
else status = "OK";

🎨 UI Style

Professional Bootstrap layout (light gray background, rounded cards).

Cards: Green = Healthy, Yellow = Due Soon, Red = Overdue.

Responsive and mobile-friendly.

🧪 Testing and Run Instructions

Provide README with:
1️⃣ cd backend && npm install && npm start
2️⃣ cd frontend && npm install && npm start
3️⃣ App runs on http://localhost:5173 (frontend) ↔ http://localhost:5000 (backend).

🧰 Libraries to Include

Backend: express, mongoose, cors, dotenv, pdfkit, nodemon

Frontend: react, react-dom, react-router-dom, axios, bootstrap, react-toastify

✅ Deliverables

Complete working full-stack app.

CRUD functionalities tested.

PDF report endpoint working.

Seed data and dummy frontend data linked.

Clear run instructions in README.

💾 After Build

Open two shells → run:

cd backend && npm start
cd frontend && npm start


Test API with Postman or frontend table.

Replace Mongo connection URL in .env with your Atlas link.

Deploy backend on Render, frontend on Vercel.